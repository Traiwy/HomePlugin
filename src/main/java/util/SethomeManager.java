package util;

import org.bukkit.event.Listener;

import java.net.http.WebSocket;
import java.util.*;

public class SethomeManager implements Listener {
    //private static Map<UUID, String> awaitingHomeName  = new HashMap();
    //private static Set<UUID> readyToSetHome = new HashSet<>();

    //public void addSethomePlayer(UUID uuid, String name){
   //    sethomePlayer.put(uuid, name);
   //}

   //public String getSethomePlayere(UUID uuid) {
   //    return sethomePlayer.get(uuid);
   //}
   //public boolean checkSethomePlayer(UUID uuid){
   //    return sethomePlayer.containsKey(uuid);
   //}
}
